# Cloudflare Pages Configuration
# This file configures Cloudflare Pages to use npm instead of bun

name = "adapti-web-charm"
compatibility_date = "2024-01-01"

[build]
command = "npm install && npm run build"
cwd = "."

